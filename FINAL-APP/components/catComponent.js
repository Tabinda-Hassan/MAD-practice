import React from 'react';
import {Text} from 'react-native';

const cat = () =>
{
    // Returning JSX that renders a Text component with a message
    return <Text>Hello, I am a student</Text>;
};
export default cat;


